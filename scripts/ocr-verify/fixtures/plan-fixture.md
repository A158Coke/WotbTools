# Fixture Current Plan (OCR verify — Case 2)

## Requirement
- [ ] Feature X: DetailPanel must display the tank image for the selected vehicle (NOT implemented anywhere in this fixture repo).
- [ ] Feature Y: SampleService must not throw on a player with no recorded score.

## Acceptance criteria
- Detail panel shows tank image when a vehicle is selected.
- getScore(absent player) returns 1 instead of throwing.
