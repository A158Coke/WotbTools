name: CI / PR
run-name: "CI / PR for ${{ github.ref_name }}"

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, ready_for_review]
  workflow_dispatch:
    inputs:
      pr_number:
        description: Open pull request number to validate
        required: true
        type: string
      head_sha:
        description: Exact current pull request head commit SHA
        required: true
        type: string

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    name: changes
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      base_sha: ${{ steps.revisions.outputs.base_sha }}
      head_sha: ${{ steps.revisions.outputs.head_sha }}
      backend: ${{ steps.plan.outputs.backend }}
      frontend: ${{ steps.plan.outputs.frontend }}
      http_contract: ${{ steps.plan.outputs.http_contract }}
      keycloak_provider: ${{ steps.plan.outputs.keycloak_provider }}
      keycloak_runtime: ${{ steps.plan.outputs.keycloak_runtime }}
      data: ${{ steps.plan.outputs.data }}
      live_data: ${{ steps.plan.outputs.live_data }}
      android: ${{ steps.plan.outputs.android }}
      deploy: ${{ steps.plan.outputs.deploy }}
      observability: ${{ steps.plan.outputs.observability }}
      full: ${{ steps.plan.outputs.full }}
      java_modules: ${{ steps.plan.outputs.java_modules }}
      java_full: ${{ steps.plan.outputs.java_full }}
      browser_suites: ${{ steps.plan.outputs.browser_suites }}
      tofu_roots: ${{ steps.plan.outputs.tofu_roots }}
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Reject tracked local development plan
        shell: bash
        run: |
          set -euo pipefail
          if git ls-files --error-unmatch -- docs/current-plan.md >/dev/null 2>&1; then
            echo "::error::docs/current-plan.md is a local-only planning artifact and must not be tracked." >&2
            exit 1
          fi
          git check-ignore -q --no-index -- docs/current-plan.md || {
            echo "::error::docs/current-plan.md must remain ignored." >&2
            exit 1
          }
      - name: Resolve and verify pull request range
        id: revisions
        env:
          CI_EVENT_NAME: ${{ github.event_name }}
          EVENT_PR_NUMBER: ${{ github.event.pull_request.number || '' }}
          EVENT_BASE_REPO: ${{ github.event.pull_request.base.repo.full_name || '' }}
          EVENT_BASE_REF: ${{ github.event.pull_request.base.ref || '' }}
          EVENT_BASE_SHA: ${{ github.event.pull_request.base.sha || '' }}
          EVENT_HEAD_SHA: ${{ github.event.pull_request.head.sha || '' }}
          DISPATCH_PR_NUMBER: ${{ inputs.pr_number || '' }}
          DISPATCH_HEAD_SHA: ${{ inputs.head_sha || '' }}
          REPOSITORY: ${{ github.repository }}
          CURRENT_SHA: ${{ github.sha }}
          CURRENT_REF: ${{ github.ref }}
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json
          import os
          import re
          import urllib.error
          import urllib.request

          sha_pattern = re.compile(r"[0-9a-f]{40}\Z")
          event = os.environ["CI_EVENT_NAME"]
          repository = os.environ["REPOSITORY"]

          def require(condition, message):
              if not condition:
                  raise SystemExit(f"::error::{message}")

          def valid_sha(value):
              return bool(sha_pattern.fullmatch(value))

          if event == "pull_request":
              base_repo = os.environ["EVENT_BASE_REPO"]
              base_ref = os.environ["EVENT_BASE_REF"]
              base_sha = os.environ["EVENT_BASE_SHA"]
              head_sha = os.environ["EVENT_HEAD_SHA"]
              pr_number = os.environ["EVENT_PR_NUMBER"]
              require(base_repo.casefold() == repository.casefold(), "PR base repository must be this repository")
              require(base_ref == "main", "PR base branch must be main")
          elif event == "workflow_dispatch":
              pr_number = os.environ["DISPATCH_PR_NUMBER"]
              requested_head = os.environ["DISPATCH_HEAD_SHA"]
              current_sha = os.environ["CURRENT_SHA"]
              current_ref = os.environ["CURRENT_REF"]
              require(pr_number.isascii() and pr_number.isdecimal() and int(pr_number) > 0,
                      "workflow_dispatch pr_number must be a positive integer")
              require(valid_sha(requested_head), "workflow_dispatch head_sha must be a full lowercase commit SHA")
              require(current_ref.startswith("refs/heads/") and len(current_ref) > len("refs/heads/"),
                      "workflow_dispatch must run from a branch")
              require(current_sha == requested_head, "workflow_dispatch head_sha is stale for this workflow run")
              token = os.environ.get("GH_TOKEN", "")
              require(bool(token), "read-only GitHub token is unavailable")
              url = f"https://api.github.com/repos/{repository}/pulls/{pr_number}"
              request = urllib.request.Request(
                  url,
                  headers={
                      "Accept": "application/vnd.github+json",
                      "Authorization": f"Bearer {token}",
                      "X-GitHub-Api-Version": "2022-11-28",
                      "User-Agent": "wotbtools-ci",
                  },
              )
              try:
                  with urllib.request.urlopen(request, timeout=20) as response:
                      pr = json.load(response)
              except (urllib.error.URLError, TimeoutError, json.JSONDecodeError) as error:
                  raise SystemExit(f"::error::Unable to read PR {pr_number} with the read-only GitHub token: {error}")
              base = pr.get("base") or {}
              head = pr.get("head") or {}
              base_repository = (base.get("repo") or {}).get("full_name", "")
              head_repository = (head.get("repo") or {}).get("full_name", "")
              head_ref = head.get("ref", "")
              base_ref = base.get("ref", "")
              base_sha = base.get("sha", "")
              head_sha = head.get("sha", "")
              current_branch = current_ref.removeprefix("refs/heads/")
              require(pr.get("state") == "open", f"PR {pr_number} must be open")
              require(base_repository.casefold() == repository.casefold(), "PR base repository must be this repository")
              require(base_ref == "main", "PR base branch must be main")
              require(head_repository.casefold() == repository.casefold(), "workflow_dispatch only accepts same-repository PR heads")
              require(head_ref == current_branch, "workflow_dispatch ref must match the PR head branch")
              require(head_sha == requested_head == current_sha,
                      "PR head, workflow_dispatch head_sha, and current workflow SHA must match")
          else:
              raise SystemExit(f"::error::Unsupported CI event {event!r}")

          require(pr_number.isascii() and pr_number.isdecimal() and int(pr_number) > 0,
                  "PR number must be a positive integer")
          require(valid_sha(base_sha), "PR base SHA must be a full lowercase commit SHA")
          require(valid_sha(head_sha), "PR head SHA must be a full lowercase commit SHA")
          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as output:
              output.write(f"base_sha={base_sha}\n")
              output.write(f"head_sha={head_sha}\n")
          PY
      - name: Select validation from the CI-only impact selector
        id: plan
        env:
          BASE_SHA: ${{ steps.revisions.outputs.base_sha }}
          HEAD_SHA: ${{ steps.revisions.outputs.head_sha }}
        shell: bash
        run: |
          set -euo pipefail
          [ -n "$BASE_SHA" ] || { echo "::error::PR base SHA is required" >&2; exit 1; }
          [ -n "$HEAD_SHA" ] || { echo "::error::PR head SHA is required" >&2; exit 1; }
          python3 scripts/ci/ci-impact.py --base "$BASE_SHA" --head "$HEAD_SHA" > ci-impact.json
          python3 - ci-impact.json <<'PY' >> "$GITHUB_OUTPUT"
          import json
          import sys

          impact = json.load(open(sys.argv[1], encoding="utf-8"))
          surfaces = impact["surfaces"]
          names = {
              "backend": "backend",
              "frontend": "frontend",
              "http_contract": "httpContract",
              "android": "android",
              "keycloak_provider": "keycloakProvider",
              "keycloak_runtime": "keycloakRuntime",
              "data": "data",
              "live_data": "liveData",
              "deploy": "deploy",
              "observability": "observability",
              "full": "full",
          }
          for output_name, surface_name in names.items():
              print(f"{output_name}={str(surfaces[surface_name]).lower()}")
          print("java_modules=" + ",".join(impact["javaModules"]))
          print("java_full=" + str(impact["javaFull"]).lower())
          print("browser_suites=" + json.dumps(impact["frontendBrowserSuites"], separators=(",", ":")))
          print("tofu_roots=" + json.dumps(impact["tofuRoots"], separators=(",", ":")))
          PY

  python_unit:
    needs: [changes]
    if: needs.changes.outputs.data == 'true' || needs.changes.outputs.full == 'true'
    name: Python unit tests
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - name: Python unit tests
        run: python3 -m unittest discover -s common/python/tests -p 'test_*.py'

  live_data_contracts:
    needs: [changes]
    if: needs.changes.outputs.live_data == 'true'
    name: Live data contracts
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - name: Validate live BlitzKit equipment contract
        run: |
          set -euo pipefail
          cp common/wotb-item-catalog-json/equipment.json /tmp/equipment.json
          python3 common/python/sync_equipment_snapshot.py --catalog /tmp/equipment.json
          diff -u common/wotb-item-catalog-json/equipment.json /tmp/equipment.json
      - name: Validate live BlitzKit Tankopedia contract
        run: |
          set -euo pipefail
          rm -rf /tmp/tankopedia-ci
          mkdir -p /tmp/tankopedia-ci
          python3 common/python/sync_tankopedia_snapshot.py \
            --existing-dir common \
            --output-dir /tmp/tankopedia-ci
          for tier in 7 8 9 10; do
            python3 -m json.tool "/tmp/tankopedia-ci/tankopedia-tier${tier}.json" >/dev/null
          done
      - name: Validate live BlitzKit crew skill contract
        run: |
          set -euo pipefail
          python3 common/python/update_crew_skills.py \
            --existing common/crew-skills.json \
            --output /tmp/crew-skills.json
          python3 -m json.tool /tmp/crew-skills.json >/dev/null
          diff -u common/crew-skills.json /tmp/crew-skills.json

  backend:
    needs: [changes]
    if: needs.changes.outputs.backend == 'true' || needs.changes.outputs.full == 'true'
    name: Backend
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "25"
      - uses: actions/cache@v4
        with:
          path: java/.m2repo
          key: ${{ runner.os }}-maven-${{ hashFiles('java/**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven-
      - name: Maven test
        working-directory: java
        env:
          JAVA_MODULES: ${{ needs.changes.outputs.java_modules }}
          JAVA_FULL: ${{ needs.changes.outputs.java_full }}
        shell: bash
        run: |
          set -euo pipefail
          if [ "$JAVA_FULL" = true ]; then
            mvn --batch-mode --no-transfer-progress -s settings-ci.xml -Dmaven.repo.local=.m2repo test
          else
            [ -n "$JAVA_MODULES" ] || { echo "::error::selected Java validation has no modules" >&2; exit 1; }
            mvn --batch-mode --no-transfer-progress -s settings-ci.xml -Dmaven.repo.local=.m2repo -pl "$JAVA_MODULES" -am test
          fi

  keycloak_providers:
    needs: [changes]
    if: needs.changes.outputs.keycloak_provider == 'true' || needs.changes.outputs.full == 'true'
    name: Keycloak providers
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - name: Official QQ provider
        working-directory: keycloak-qq-provider
        run: mvn --batch-mode --no-transfer-progress -s ../java/settings-ci.xml -Dmaven.repo.local=../java/.m2repo test
      - name: Wargaming provider
        working-directory: keycloak-wargaming-provider
        run: mvn --batch-mode --no-transfer-progress -s ../java/settings-ci.xml -Dmaven.repo.local=../java/.m2repo test
      - name: Validate Keycloak provider/runtime safety contract
        run: bash scripts/ci/test-keycloak-tofu-contract.sh

  keycloak_runtime:
    needs: [changes]
    if: needs.changes.outputs.keycloak_runtime == 'true' || needs.changes.outputs.full == 'true'
    name: Keycloak runtime
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - name: Validate Keycloak runtime inputs
        run: |
          set -euo pipefail
          bash -n docker/keycloak/wotbtools-entrypoint.sh

  frontend:
    needs: [changes]
    if: needs.changes.outputs.frontend == 'true' || needs.changes.outputs.full == 'true'
    name: Frontend
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install dependencies
        working-directory: frontend
        run: npm ci --no-audit --no-fund
      - name: Type check
        working-directory: frontend
        run: npm run typecheck
      - name: Unit tests
        working-directory: frontend
        run: npm test
      - name: Real-browser Playback layout regression
        if: contains(fromJSON(needs.changes.outputs.browser_suites), 'playback-layout')
        working-directory: frontend
        run: npm run test:browser-layout
      - name: Real-browser Playback/Workspace interaction regression
        if: contains(fromJSON(needs.changes.outputs.browser_suites), 'workspace-interaction')
        working-directory: frontend
        run: npm run test:browser-interaction
      - name: Build
        working-directory: frontend
        run: npm run build
      - name: Bundle separation check
        working-directory: frontend
        run: node scripts/check-bundle-separation.mjs

  http_contract:
    needs: [changes]
    if: needs.changes.outputs.http_contract == 'true' || needs.changes.outputs.full == 'true'
    name: HTTP contract
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install frontend contract tooling
        working-directory: frontend
        run: npm ci --no-audit --no-fund
      - name: Validate OpenAPI and local refs
        working-directory: frontend
        run: npm run api:lint
      - name: Verify generated frontend contract is current
        working-directory: frontend
        run: npm run api:check
      - name: Validate shared Playback fixture
        working-directory: frontend
        run: npm run api:fixture
      - name: Runtime contract tests
        working-directory: frontend
        run: npx vitest run src/api/contract-runtime.test.ts
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "25"
      - name: Backend Playback serialization contract tests
        working-directory: java
        run: |
          set -euo pipefail
          mvn --batch-mode --no-transfer-progress -s settings-ci.xml -Dmaven.repo.local=.m2repo -pl wotb-web -am -DskipTests install
          mvn --batch-mode --no-transfer-progress -s settings-ci.xml -Dmaven.repo.local=.m2repo -pl wotb-web -Dtest=BattlePlaybackDatasetTest,BattlePlaybackProjectorTest,ReplayArtifactWriterTest -Dsurefire.failIfNoSpecifiedTests=true test

  android:
    needs: [changes]
    if: needs.changes.outputs.android == 'true' || needs.changes.outputs.full == 'true'
    name: Android
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.7"
      - name: Test bounded CI network retry helper
        run: bash scripts/ci/test-network-retry.sh
      - name: Accept Android SDK licenses (best-effort)
        run: |
          if [ -x "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" ]; then
            yes | "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" --licenses >/dev/null 2>&1 || true
          fi
      - name: Assemble debug APK and run Android JVM unit tests
        working-directory: android
        run: bash ../scripts/ci/run-with-network-retry.sh "Android Gradle dependency resolution" gradle :app:assembleDebug :app:testDebugUnitTest --no-daemon
      - name: Upload debug APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error

  android_release_helpers:
    needs: [changes]
    if: needs.changes.outputs.android == 'true' || needs.changes.outputs.full == 'true'
    name: Android release helpers
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
      - name: Shell syntax check
        run: |
          set -euo pipefail
          bash -n scripts/android-release/resolve-version.sh
          bash -n scripts/android-release/check-release-guards.sh
          bash -n scripts/android-release/test-release.sh
      - name: Release helper unit tests
        run: bash scripts/android-release/test-release.sh

  android_contract:
    needs: [changes]
    if: needs.changes.outputs.android == 'true' || needs.changes.outputs.full == 'true'
    name: Android contract
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Validate committed Android contract
        run: |
          set -euo pipefail
          python3 scripts/android-release/android_contract.py validate \
            --contract contracts/android-native-bridge.json \
            --gradle-properties android/gradle.properties \
            --frontend frontend/src/platform/nativeBridgeContract.js \
            --native-source android/app/src/main/java/com/wotbtools/app/NativeBridge.kt \
            --native-source android/app/src/main/java/com/wotbtools/app/MainActivity.kt \
            --native-source android/app/src/main/java/com/wotbtools/app/ReplayIntentHandler.kt
          bash scripts/android-release/test-android-contract.sh
      - name: Enforce version and bridge compatibility against base
        env:
          BASE_SHA: ${{ needs.changes.outputs.base_sha }}
          HEAD_SHA: ${{ needs.changes.outputs.head_sha }}
        run: |
          set -euo pipefail
          git show "$BASE_SHA:android/gradle.properties" > /tmp/base-gradle.properties
          if git cat-file -e "$BASE_SHA:contracts/android-native-bridge.json" 2>/dev/null; then
            git show "$BASE_SHA:contracts/android-native-bridge.json" > /tmp/base-android-native-bridge.json
          else
            # First introduction of the contract has no historical JSON baseline;
            # validate the committed head contract now and start breaking diffs
            # from this SSOT on the next PR.
            cp contracts/android-native-bridge.json /tmp/base-android-native-bridge.json
          fi
          git diff --name-only "$BASE_SHA" "$HEAD_SHA" > /tmp/android-changed-paths.txt
          BASE_VERSION="$(sed -n 's/^wotbVersion=//p' /tmp/base-gradle.properties | head -n1)"
          HEAD_VERSION="$(sed -n 's/^wotbVersion=//p' android/gradle.properties | head -n1)"
          INITIAL_VERSION_BASELINE=false
          if [ -z "$BASE_VERSION" ]; then
            # First introduction of Version-as-Code has no historical version;
            # use the committed head as the initial baseline.
            BASE_VERSION="$HEAD_VERSION"
            INITIAL_VERSION_BASELINE=true
          fi
          FE_VERSIONS="$(python3 - <<'PY'
          import re
          text = open('frontend/src/platform/nativeBridgeContract.js', encoding='utf-8').read()
          match = re.search(r'SUPPORTED_NATIVE_BRIDGE_VERSIONS\s*=\s*Object\.freeze\(\[([^\]]*)\]\)', text)
          if not match:
              raise SystemExit('frontend supported Native Bridge versions declaration is missing')
          print(','.join(re.findall(r'\b[0-9]+\b', match.group(1))))
          PY
          )"
          python3 scripts/android-release/android_contract.py gate \
            --base-contract /tmp/base-android-native-bridge.json \
            --head-contract contracts/android-native-bridge.json \
            --base-version "$BASE_VERSION" \
            --head-version "$HEAD_VERSION" \
            --paths /tmp/android-changed-paths.txt \
            --frontend-versions "$FE_VERSIONS" \
            $([ "$INITIAL_VERSION_BASELINE" = true ] && echo --initial-version-baseline)

  observability_config:
    needs: [changes]
    if: needs.changes.outputs.observability == 'true' || needs.changes.outputs.full == 'true'
    name: Observability
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - name: Pull Prometheus image with bounded network retry
        run: bash scripts/ci/run-with-network-retry.sh "Pull Prometheus image" docker pull prom/prometheus:v2.55.1
      - name: Pull Loki image with bounded network retry
        run: bash scripts/ci/run-with-network-retry.sh "Pull Loki image" docker pull grafana/loki:3.3.2
      - name: Pull Alloy image with bounded network retry
        run: bash scripts/ci/run-with-network-retry.sh "Pull Alloy image" docker pull grafana/alloy:v1.4.2
      - name: Prometheus config validation
        run: >-
          docker run --rm --entrypoint promtool
          -v "$PWD/deploy/observability/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro"
          prom/prometheus:v2.55.1 check config /etc/prometheus/prometheus.yml
      - name: Loki config validation
        run: >-
          docker run --rm --entrypoint loki
          -v "$PWD/deploy/observability/loki/loki-config.yml:/etc/loki/loki-config.yml:ro"
          grafana/loki:3.3.2 --config.file=/etc/loki/loki-config.yml --verify-config
      - name: Alloy config format check
        run: >-
          docker run --rm --entrypoint alloy
          -v "$PWD/deploy/observability/alloy/config.alloy:/etc/alloy/config.alloy:ro"
          grafana/alloy:v1.4.2 fmt -t /etc/alloy/config.alloy
      - name: Alloy production selector contract
        run: bash deploy/test-alloy-config.sh
      - name: Runtime Docker emitter to Alloy to Loki smoke
        run: bash deploy/test-observability-e2e.sh
      - name: Grafana runtime provisioning smoke
        run: bash deploy/test-grafana-runtime.sh
      - name: Validate Grafana provisioning and dashboards
        run: |
          set -euo pipefail
          python - <<'PY'
          import glob, json, os, yaml

          # Panel links of type=dashboard + uid without a valid url crash the Grafana
          # frontend (TypeError: Cannot read properties of undefined (reading 'replace')).
          # Forbid this structure so the dashboard runtime stays stable.
          required = {
              "wotbtools-production-overview.json",
              "wotbtools-backend-overview.json",
              "wotbtools-ai-review.json",
              "wotbtools-keycloak.json",
              "wotbtools-error-explorer.json",
              "wotbtools-usage.json",
          }
          paths = glob.glob("deploy/observability/grafana/dashboards/*.json")
          assert {p.replace("\\", "/").rsplit("/", 1)[-1] for p in paths} == required, "dashboard inventory drift"
          dashboard_uids = {}
          dashboard_link_uids = []
          production_health_contract = {
              "后端服务": 'up{job="wotb-backend"}',
              "主机": 'up{job="node-exporter"}',
              "Prometheus": 'up{job="prometheus"}',
              "Loki": 'up{job="loki"}',
              "Grafana": 'up{job="grafana"}',
          }

          def validate_datasource(value, where):
              if not isinstance(value, dict):
                  return
              uid = value.get("uid")
              if uid is not None:
                  assert uid in {"prometheus", "loki"}, f"{where}: unknown datasource uid {uid}"

          def walk_dashboard_panels(node, where):
              if not isinstance(node, dict):
                  return
              validate_datasource(node.get("datasource"), where)
              for target_index, target in enumerate(node.get("targets") or []):
                  if isinstance(target, dict):
                      validate_datasource(target.get("datasource"), f"{where}.targets[{target_index}]")
                      expr = target.get("expr") or target.get("query") or ""
                      if "or vector(0)" in expr:
                          assert not any(token in expr for token in ("up(", "queue_depth", "in_flight", "node_")), \
                              f"{where}: health/gauge query must not mask no-data with vector(0): {expr}"
              for link in node.get("links") or []:
                  if isinstance(link, dict) and link.get("type") == "dashboard" and isinstance(link.get("uid"), str):
                      dashboard_link_uids.append(link["uid"])
                      url = link.get("url")
                      if not isinstance(url, str) or not url.strip():
                          raise SystemExit(
                              f"{where}: panel link type=dashboard with uid but no valid url: {link}"
                          )
              for key in ("panels", "rows"):
                  for i, child in enumerate(node.get(key) or []):
                      walk_dashboard_panels(child, f"{where}.{key}[{i}]")

          def collect_production_panels(node, found):
              if not isinstance(node, dict):
                  return
              title = node.get("title")
              if isinstance(title, str):
                  found.setdefault(title, []).extend(
                      target.get("expr", "")
                      for target in node.get("targets") or []
                      if isinstance(target, dict) and isinstance(target.get("expr"), str)
                  )
              for key in ("panels", "rows"):
                  for child in node.get(key) or []:
                      collect_production_panels(child, found)

          for p in glob.glob("deploy/observability/grafana/provisioning/**/*.yml", recursive=True):
              yaml.safe_load(open(p, encoding="utf-8"))
          assert not os.path.exists("deploy/observability/grafana/provisioning/dashboards/dashboards.yml"), \
              "dashboard file-provisioning controller must remain disabled after OpenTofu adoption"
          for p in paths:
              d = json.load(open(p, encoding="utf-8"))
              assert d.get("panels"), "empty dashboard " + p
              uid = d.get("uid")
              assert isinstance(uid, str) and uid, "dashboard uid missing: " + p
              assert uid not in dashboard_uids.values(), "duplicate dashboard uid: " + uid
              dashboard_uids[p] = uid
              if p.replace("\\", "/").endswith("wotbtools-production-overview.json"):
                  production_panels = {}
                  collect_production_panels(d, production_panels)
                  for title, expected_expr in production_health_contract.items():
                      actual_exprs = {"".join(expr.split()) for expr in production_panels.get(title, [])}
                      assert expected_expr in actual_exprs, \
                          f"production overview health panel {title} must query {expected_expr}"
                      assert all("orvector(0)" not in expr for expr in actual_exprs), \
                          f"production overview health panel {title} must not mask no-data"
                  overall_exprs = [
                      expr for title, exprs in production_panels.items()
                      if title.startswith("生产状态")
                      for expr in exprs
                  ]
                  assert overall_exprs, "production overview overall health panel missing"
                  overall_expr = "".join(overall_exprs[0].split())
                  required_jobs = "wotb-backend|node-exporter|prometheus|loki|grafana"
                  expected_overall = (
                      f'min(up{{job=~"{required_jobs}"}})and('
                      f'count(up{{job=~"{required_jobs}"}})==5)'
                  )
                  assert overall_expr == expected_overall, \
                      "production overview overall health must use min(up) as the value and count(up)==5 as the presence guard"
                  assert "orvector(0)" not in overall_expr, \
                      "production overview overall health must not mask no-data"
              for i, panel in enumerate(d.get("panels", [])):
                  walk_dashboard_panels(panel, f"{p}::panels[{i}]")
          uid_set = set(dashboard_uids.values())
          assert len(uid_set) == len(paths), "dashboard UIDs must be unique"
          assert set(dashboard_link_uids) <= uid_set, "dashboard link points to an unknown uid"
          print("provisioning yml + dashboards json OK")
          PY
  deploy_smoke:
    needs: [changes]
    if: needs.changes.outputs.deploy == 'true' || needs.changes.outputs.full == 'true'
    name: Deployment and infrastructure contracts
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Flyway migration immutability guard
        env:
          FLYWAY_BASE_REF: ${{ needs.changes.outputs.base_sha }}
        run: bash deploy/check-flyway-immutability.sh "$FLYWAY_BASE_REF"
      - name: Flyway migration guard fixture
        run: bash deploy/test-flyway-immutability.sh
      - name: Read-only dependency readiness protocol unit tests
        run: python3 deploy/test_dependency_readiness.py
      - name: Bash syntax for deploy entrypoints and local policy fixtures
        run: >-
          bash -n deploy/deploy.sh deploy/minio-deploy.sh
          deploy/verify-observability.sh deploy/tx/deploy.sh
          deploy/tx/business-postgres-backup.sh deploy/tx/business-postgres-restore.sh
          deploy/tx/runtime-check.sh deploy/tx/publish-loaded-image-to-tcr.sh
          deploy/test-tx-runtime-config.sh deploy/test-user-profile-reset-audit.sh
          deploy/test-yecao-wireguard-boundary.sh deploy/test-tx-runtime-check.sh
          deploy/test-deploy-contract.sh deploy/test-nginx-grafana-recreate.sh
          infra/tofu/minio/validate-plan.sh infra/tofu/minio/test-validate-plan.sh
          infra/tofu/rabbitmq/validate-plan.sh infra/tofu/rabbitmq/test-validate-plan.sh
          infra/tofu/postgres-business/validate-plan.sh infra/tofu/postgres-business/test-validate-plan.sh
          scripts/ci/test-keycloak-tofu-contract.sh
          scripts/ci/test-postgres-keycloak-tofu-contract.sh
          scripts/ci/test-postgres-business-tofu-contract.sh
          scripts/ci/test-tofu-workflow-contract.sh
      - name: CI selector, workflow purity, and ToFu safety fixtures
        run: |
          set -euo pipefail
          python3 scripts/ci/test-path-filters.py
          bash scripts/ci/test-workflow-contract.sh
          bash scripts/ci/test-tofu-workflow-contract.sh
          bash scripts/ci/test-tofu-prod-plan-guard.sh
          bash scripts/ci/test-keycloak-tofu-contract.sh
          bash scripts/ci/test-postgres-keycloak-tofu-contract.sh
          bash scripts/ci/test-postgres-business-tofu-contract.sh
      - name: TX runtime and metadata fixtures
        run: |
          set -euo pipefail
          bash deploy/test-tx-runtime-config.sh
          bash deploy/test-yecao-wireguard-boundary.sh
          bash deploy/test-tx-runtime-check.sh
          bash deploy/test-user-profile-reset-audit.sh
          bash deploy/test-deploy-contract.sh
      - name: Production Compose configuration contract
        env:
          TAG: sha-ci
          DB_PASSWORD: ci
          KC_ADMIN_PASSWORD: ci
          WG_APPLICATION_ID: ci
          KEYCLOAK_ADMIN_CLIENT_SECRET: ci
          AI_API_KEY: ci
          GRAFANA_ADMIN_USER: ci
          GRAFANA_ADMIN_PASSWORD: ci
        run: |
          set -euo pipefail
          docker compose -f deploy/docker-compose.prod.yml config --format json > /tmp/compose.json
          python3 - <<'PY'
          import json
          with open('/tmp/compose.json', encoding='utf-8') as compose_file:
              services = json.load(compose_file)['services']
          endpoint = services['parser-worker']['environment']['MINIO_ENDPOINT']
          assert endpoint == 'minio:9000', endpoint
          print('parser-worker resolves MinIO through Docker service discovery')
          PY
      - name: MinIO Compose configuration contract
        env:
          TAG: sha-ci
          YECAO_MINIO_ROOT_USER: ci-root-user
          YECAO_MINIO_ROOT_PASSWORD: ci-root-password
        run: |
          set -euo pipefail
          docker compose -f deploy/docker-compose.minio.yml config --format json > /tmp/minio-compose.json
          python3 - <<'PY'
          import json
          with open('/tmp/minio-compose.json', encoding='utf-8') as compose_file:
              compose = json.load(compose_file)
          assert set(compose['services']) == {'minio'}
          service = compose['services']['minio']
          assert service['image'] == 'ghcr.io/a158coke/wotbtools-minio:sha-ci'
          ports = [str(port) for port in service.get('ports', [])]
          assert any('10.20.0.2' in port and '9000' in port for port in ports), ports
          assert any('127.0.0.1' in port and '9001' in port for port in ports), ports
          assert not any('0.0.0.0' in port for port in ports), ports
          assert service.get('healthcheck'), 'MinIO healthcheck is required'
          assert compose['volumes']['minio_data']['name'] == 'wotb_yecao_minio_data'
          assert compose['networks']['default']['external'] is True
          print('MinIO Compose network and persistence contract OK')
          PY
      - name: nginx rate-limit fixture smoke
        run: bash deploy/test-nginx-ratelimit.sh
      - name: nginx Android download route fixture smoke
        run: bash deploy/test-nginx-android-route.sh
      - name: nginx Grafana recreate fixture smoke
        run: bash deploy/test-nginx-grafana-recreate.sh
  tofu_plans:
    needs: [changes]
    if: needs.changes.outputs.tofu_roots != '[]'
    name: OpenTofu validation / ${{ matrix.root }}
    runs-on: ubuntu-latest
    timeout-minutes: 35
    # PR OpenTofu work is local validation only: fmt, init without a backend,
    # validate, and root-specific safety fixtures. No production plan or credentials
    # are available to this workflow.
    strategy:
      fail-fast: false
      matrix:
        root: ${{ fromJSON(needs.changes.outputs.tofu_roots) }}
    env:
      TF_IN_AUTOMATION: "true"
      CHECKPOINT_DISABLE: "1"
    steps:
      - uses: actions/checkout@v5
      - uses: opentofu/setup-opentofu@v2
        with:
          tofu_version: 1.12.6
          tofu_wrapper: false
      - name: Resolve one declared root
        id: root
        env:
          ROOT: ${{ matrix.root }}
        shell: bash
        run: |
          set -euo pipefail
          case "$ROOT" in
            keycloak) path=infra/tofu/keycloak ;;
            rabbitmq) path=infra/tofu/rabbitmq ;;
            business-postgres) path=infra/tofu/postgres-business ;;
            keycloak-postgres) path=infra/tofu/postgres-keycloak ;;
            minio) path=infra/tofu/minio ;;
            cos) path=infra/tofu/environments/prod ;;
            grafana) path=infra/tofu/grafana ;;
            *) echo "::error::unknown OpenTofu root" >&2; exit 1 ;;
          esac
          echo "path=$path" >> "$GITHUB_OUTPUT"
      - name: Format, initialize without production state, and validate
        env:
          ROOT_DIR: ${{ steps.root.outputs.path }}
        shell: bash
        run: |
          set -euo pipefail
          cd "$ROOT_DIR"
          tofu fmt -check -recursive
          tofu init -backend=false -input=false
          tofu validate
      - name: Validate local-root safety policy fixtures
        env:
          ROOT: ${{ matrix.root }}
          ROOT_DIR: ${{ steps.root.outputs.path }}
        shell: bash
        run: |
          set -euo pipefail
          case "$ROOT" in
            rabbitmq|minio|business-postgres)
              bash "$ROOT_DIR/test-validate-plan.sh"
              ;;
            keycloak|keycloak-postgres|cos|grafana) ;;
            *) exit 1 ;;
          esac
  required:
    needs:
      - changes
      - python_unit
      - live_data_contracts
      - backend
      - keycloak_providers
      - keycloak_runtime
      - frontend
      - http_contract
      - android
      - android_release_helpers
      - android_contract
      - observability_config
      - deploy_smoke
      - tofu_plans
    if: always()
    name: CI / Required Gate
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Require every selected job to pass
        env:
          CHANGES: ${{ needs.changes.result }}
          FULL: ${{ needs.changes.outputs.full }}
          PYTHON_UNIT: ${{ needs.python_unit.result }}
          DATA_CHANGED: ${{ needs.changes.outputs.data }}
          LIVE_DATA_CONTRACTS: ${{ needs.live_data_contracts.result }}
          LIVE_DATA_CHANGED: ${{ needs.changes.outputs.live_data }}
          BACKEND: ${{ needs.backend.result }}
          BACKEND_CHANGED: ${{ needs.changes.outputs.backend }}
          KEYCLOAK_PROVIDERS: ${{ needs.keycloak_providers.result }}
          KEYCLOAK_PROVIDER_CHANGED: ${{ needs.changes.outputs.keycloak_provider }}
          KEYCLOAK_RUNTIME: ${{ needs.keycloak_runtime.result }}
          KEYCLOAK_RUNTIME_CHANGED: ${{ needs.changes.outputs.keycloak_runtime }}
          FRONTEND: ${{ needs.frontend.result }}
          FRONTEND_CHANGED: ${{ needs.changes.outputs.frontend }}
          HTTP_CONTRACT: ${{ needs.http_contract.result }}
          HTTP_CONTRACT_CHANGED: ${{ needs.changes.outputs.http_contract }}
          ANDROID: ${{ needs.android.result }}
          ANDROID_CHANGED: ${{ needs.changes.outputs.android }}
          ANDROID_RELEASE_HELPERS: ${{ needs.android_release_helpers.result }}
          ANDROID_CONTRACT: ${{ needs.android_contract.result }}
          OBSERVABILITY: ${{ needs.observability_config.result }}
          OBSERVABILITY_CHANGED: ${{ needs.changes.outputs.observability }}
          DEPLOY_SMOKE: ${{ needs.deploy_smoke.result }}
          DEPLOY_CHANGED: ${{ needs.changes.outputs.deploy }}
          TOFU_PLANS: ${{ needs.tofu_plans.result }}
          TOFU_ROOTS: ${{ needs.changes.outputs.tofu_roots }}
        shell: bash
        run: |
          set -euo pipefail
          failed=0
          while IFS='|' read -r job expected result; do
            case "$expected:$result" in
              true:success) ;;
              false:skipped) ;;
              *) echo "::error::$job expected=$expected ended with $result"; failed=1 ;;
            esac
          done <<EOF
          changes|true|$CHANGES
          python_unit|$([ "$DATA_CHANGED" = true ] || [ "$FULL" = true ] && echo true || echo false)|$PYTHON_UNIT
          live_data_contracts|$([ "$LIVE_DATA_CHANGED" = true ] && echo true || echo false)|$LIVE_DATA_CONTRACTS
          backend|$([ "$BACKEND_CHANGED" = true ] || [ "$FULL" = true ] && echo true || echo false)|$BACKEND
          keycloak_providers|$([ "$KEYCLOAK_PROVIDER_CHANGED" = true ] || [ "$FULL" = true ] && echo true || echo false)|$KEYCLOAK_PROVIDERS
          keycloak_runtime|$([ "$KEYCLOAK_RUNTIME_CHANGED" = true ] || [ "$FULL" = true ] && echo true || echo false)|$KEYCLOAK_RUNTIME
          frontend|$([ "$FRONTEND_CHANGED" = true ] || [ "$FULL" = true ] && echo true || echo false)|$FRONTEND
          http_contract|$([ "$HTTP_CONTRACT_CHANGED" = true ] || [ "$FULL" = true ] && echo true || echo false)|$HTTP_CONTRACT
          android|$([ "$ANDROID_CHANGED" = true ] || [ "$FULL" = true ] && echo true || echo false)|$ANDROID
          android_release_helpers|$([ "$ANDROID_CHANGED" = true ] || [ "$FULL" = true ] && echo true || echo false)|$ANDROID_RELEASE_HELPERS
          android_contract|$([ "$ANDROID_CHANGED" = true ] || [ "$FULL" = true ] && echo true || echo false)|$ANDROID_CONTRACT
          observability|$([ "$OBSERVABILITY_CHANGED" = true ] || [ "$FULL" = true ] && echo true || echo false)|$OBSERVABILITY
          deploy_smoke|$([ "$DEPLOY_CHANGED" = true ] || [ "$FULL" = true ] && echo true || echo false)|$DEPLOY_SMOKE
          tofu_plans|$([ "$TOFU_ROOTS" != '[]' ] && echo true || echo false)|$TOFU_PLANS
          EOF
          [ "$failed" -eq 0 ]
