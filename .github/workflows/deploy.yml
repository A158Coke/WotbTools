name: Build & Deploy
run-name: Deploy ${{ github.ref_name }} by @${{ github.actor }} to Production

# 同一时刻只跑一个部署; 新 push 取消进行中的旧 run, 避免 VPS 上并发部署打架
concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [main]
    # 只在影响镜像的改动上触发 (纯文档 push 不重建)
    paths:
      - "java/**"
      - "common/**"
      - "Dockerfile"
      - "deploy/**"
      - ".github/workflows/deploy.yml"
  workflow_dispatch:   # 允许手动触发 (例如仅想重新部署)

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: a158coke   # Docker Hub 用户名非机密(已出现在镜像名中); 仅 token 用 secret. 这里硬编码写死了docker用户名， 未来可以从 secrets 中获取
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Set up Builds
        uses: docker/setup-buildx-action@v3

      - name: Compute image tag
        id: vars
        run: echo "sha=sha-$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"

      - name: Build and push (with layer cache)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          push: true
          tags: |
            a158coke/wotbtool:${{ steps.vars.outputs.sha }}
            a158coke/wotbtool:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          port: ${{ secrets.VPS_PORT }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -e
            mkdir -p /opt/wotb
            cd /opt/wotb
            # 清掉 PR 前遗留的双容器(占用 8087/8088), 否则新单容器无法绑定端口
            docker rm -f wotb-backend wotb-frontend 2>/dev/null || true
            cat > docker-compose.yml <<'EOF'
            services:
              wotb:
                image: a158coke/wotbtool:${{ steps.vars.outputs.sha }}
                container_name: wotb
                ports:
                  - "8088:80"
                restart: unless-stopped
            EOF
            docker compose pull
            docker compose up -d --remove-orphans
            docker image prune -f
